apiVersion: v1
kind: Service

metadata:
  name: payment-service

spec:
  type: ClusterIP

  selector:
    app: payment-service
    version: blue

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5002