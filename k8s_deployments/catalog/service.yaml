apiVersion: v1
kind: Service

metadata:
  name: catalog-service

spec:
  type: ClusterIP

  selector:
    app: catalog-service
    version: green

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5050