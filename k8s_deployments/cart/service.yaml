apiVersion: v1
kind: Service

metadata:
  name: cart-service

spec:
  type: ClusterIP

  selector:
    app: cart-service
    version: green

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5001